<div id="view-all">
  <h2>All Characters</h2>
  <ul id="view-all-list" class="list-group">
    <table>
      {{#each character}}
      <tr data-id="{{this.id}}" class="list-group-item">
        <td>{{name}}</td>
        <td>{{race}}</td>
        <td>{{class}}</td>
        <td>{{level}}</td>
      </tr>
      {{/each}}
    </table>
  </ul>